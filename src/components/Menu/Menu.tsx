import "@/styles/components/_menu.scss";
import MenuContent from "./MenuContent";

const Menu = () => {
  return (
    <section id="menu">
      <MenuContent />
    </section>
  );
};

export default Menu;
